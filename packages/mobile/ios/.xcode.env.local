# Local environment overrides for Xcode builds
# This file is not versioned and specific to your machine

# Load nvm and use the correct Node version
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# Set the NODE_BINARY path explicitly
export NODE_BINARY=/Users/mertyildiz/.nvm/versions/node/v22.21.0/bin/node
